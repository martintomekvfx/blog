---
title: "as"
description: "as"
pubDate: "2026-02-16"
tags: ["more","coje","kundo"]
draft: true
---

## More code examples

```javascript
// Array manipulation
const numbers = [1, 2, 3, 4, 5];

const doubled = numbers.map(n => n * 2);
console.log("Doubled:", doubled.join(", "));

const sum = numbers.reduce((a, b) => a + b, 0);
console.log("Sum:", sum);
```